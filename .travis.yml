language: generic
script:
- mkdir out
- echo "$DEPLOY_KEY"
- echo Deployed at $(date) > out/index.html
- echo Success!
deploy:
  provider: pages
  edge:
    source: jyasskin/dpl
    branch: github-deploy-token
  skip_cleanup: true
  on:
    branch: master
  local_dir: out
  deploy_key: "$DEPLOY_KEY"
